name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install Dependencies
      working-directory: ./massage-mobile-app
      run: npm install

    - name: Build Web App
      working-directory: ./massage-mobile-app
      run: npm run build

    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v2

    - name: Build Capacitor Android App
      working-directory: ./massage-mobile-app
      run: |
        npx cap add android
        npx cap sync android

    - name: Build APK
      working-directory: ./massage-mobile-app/android
      run: ./gradlew assembleDebug

    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: MassageScheduler-Android
        path: massage-mobile-app/android/app/build/outputs/apk/debug/app-debug.apk
